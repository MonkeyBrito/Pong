#pragma once

typedef struct GlobalData {
    int screen_size_x;
    int screen_size_y;
} GlobalData;

typedef struct Transform {
    float x_pos;
    float y_pos;
    float size_x;
    float size_y;
} Transform;